/**
 * Data providers index.
 * Each provider knows how to convert its data format to NormalizedRelease.
 */

export { normalizeWPVersionStats } from './wp-version';
export { normalizeGBReleases } from './gb-release';
